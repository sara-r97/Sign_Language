
var soundA, soundB, soundC, soundD, soundE, soundF, soundG, soundH, soundI, soundJ, soundK, soundL, soundM;
var soundN, soundO, soundP, soundQ, soundR, soundS, soundT, soundU, soundV, soundW, soundX, soundY, soundZ;
var listener, audioLoader;
var vol = 0.5;

function sounds(){
    
    audioLoader = new THREE.AudioLoader();

    //A
    soundA = new THREE.Audio(listener);
    audioLoader.load('sound/A.m4a', function(buffer){
        soundA.setBuffer(buffer);
    });
    //B
    soundB = new THREE.Audio(listener);
    audioLoader.load('sound/B.m4a', function(buffer){
        soundB.setBuffer(buffer);
    });
    //C
    soundC = new THREE.Audio(listener);
    audioLoader.load('sound/C.m4a', function(buffer){
        soundC.setBuffer(buffer);
    });
    //D
    soundD = new THREE.Audio(listener);
    audioLoader.load('sound/D.m4a', function(buffer){
        soundD.setBuffer(buffer);
    });
    //E
    soundE = new THREE.Audio(listener);
    audioLoader.load('sound/E.m4a', function(buffer){
        soundE.setBuffer(buffer);
    });
    //F
    soundF = new THREE.Audio(listener);
    audioLoader.load('sound/F.m4a', function(buffer){
        soundF.setBuffer(buffer);
    });
    //G
    soundG = new THREE.Audio(listener);
    audioLoader.load('sound/G.m4a', function(buffer){
        soundG.setBuffer(buffer);
    });
    //H
    soundH = new THREE.Audio(listener);
    audioLoader.load('sound/H.m4a', function(buffer){
        soundH.setBuffer(buffer);
    });
    //I
    soundI = new THREE.Audio(listener);
    audioLoader.load('sound/I.m4a', function(buffer){
        soundI.setBuffer(buffer);
    });
    //J
    soundJ = new THREE.Audio(listener);
    audioLoader.load('sound/J.m4a', function(buffer){
        soundJ.setBuffer(buffer);
    });
    //K
    soundK = new THREE.Audio(listener);
    audioLoader.load('sound/K.m4a', function(buffer){
        soundK.setBuffer(buffer);
    });
    //L
    soundL = new THREE.Audio(listener);
    audioLoader.load('sound/L.m4a', function(buffer){
        soundL.setBuffer(buffer);
    });
    //M
    soundM = new THREE.Audio(listener);
    audioLoader.load('sound/M.m4a', function(buffer){
        soundM.setBuffer(buffer);
    });
    //N
    soundN = new THREE.Audio(listener);
    audioLoader.load('sound/N.m4a', function(buffer){
        soundN.setBuffer(buffer);
    });
    //O
    soundO = new THREE.Audio(listener);
    audioLoader.load('sound/O.m4a', function(buffer){
        soundO.setBuffer(buffer);
    });
    //P
    soundP = new THREE.Audio(listener);
    audioLoader.load('sound/P.m4a', function(buffer){
        soundP.setBuffer(buffer);
    });
    //Q
    soundQ = new THREE.Audio(listener);
    audioLoader.load('sound/Q.m4a', function(buffer){
        soundQ.setBuffer(buffer);
    });
    //R
    soundR = new THREE.Audio(listener);
    audioLoader.load('sound/R.m4a', function(buffer){
        soundR.setBuffer(buffer);
    });
    //S
    soundS = new THREE.Audio(listener);
    audioLoader.load('sound/S.m4a', function(buffer){
        soundS.setBuffer(buffer);
    });
    //T
    soundT = new THREE.Audio(listener);
    audioLoader.load('sound/T.m4a', function(buffer){
        soundT.setBuffer(buffer);
    });
    //U
    soundU = new THREE.Audio(listener);
    audioLoader.load('sound/U.m4a', function(buffer){
        soundU.setBuffer(buffer);
    });
    //V
    soundV = new THREE.Audio(listener);
    audioLoader.load('sound/V.m4a', function(buffer){
        soundV.setBuffer(buffer);
    });
    //W
    soundW = new THREE.Audio(listener);
    audioLoader.load('sound/W.m4a', function(buffer){
        soundW.setBuffer(buffer);
    });
    //X
    soundX = new THREE.Audio(listener);
    audioLoader.load('sound/X.m4a', function(buffer){
        soundX.setBuffer(buffer);
    });
    //Y
    soundY = new THREE.Audio(listener);
    audioLoader.load('sound/Y.m4a', function(buffer){
        soundY.setBuffer(buffer);
    });
    //Z
    soundZ = new THREE.Audio(listener);
    audioLoader.load('sound/Z.m4a', function(buffer){
        soundZ.setBuffer(buffer);
    });

}
